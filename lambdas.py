def addTheses(x, y): 
	return x + y

addTheseLambda = lambda x, y: x + y

print addTheses(5, 9)
print addTheseLambda(5, 9)

for key in sorted(mydict, key=lambda key: mydict[key]):
	print "{0} = {1}".format(key, mydict[key])

def by_dict_lname(this_dict): 
	return this_dict[p]

for key 